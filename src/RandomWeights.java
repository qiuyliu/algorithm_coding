public class RandomWeights {
    // private int sum;
    // private int[] presum;

    
    // public int pickIndex() {
    //     double picked = sum * Math.random();
    //     int left = 0, right = this.presum.length - 1;
    //     while (left < right) {
    //         int mid = left + (right - left) / 2;
    //         if (presum[mid] > picked) {
    //             right = mid;
    //         } else {
    //             left = mid + 1;
    //         }
    //     }
    //     return left;
    // }
    // public RandomWeights() {
    //     this.presum = new int[w.length];
    //     int prefixsum = 0;
    //     for (int i = 0; i < w.length; i++) {
    //         prefixsum += w[i];
    //         this.presum[i] = prefixsum;
    //     }
    //     this.sum = prefixsum;
    // }

    // public boolean set(String s, int k) {

    // }

    // public int get() {

    // }

}
